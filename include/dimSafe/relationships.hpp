#pragma once

namespace dimSafe {

template<typename LeftQuantity, typename RightQuantity>
struct DivideRule;

template<typename LeftQuantity, typename RightQuantity>
struct MultiplyRule;

} // namespace dimSafe
