#pragma once

#include "base_dimesions.hpp"

namespace dimSafe {

using HeatCapacityDimension = Dimension<2, 1, -2, 0, -1, 0, 0>;
using EntropyDimension = Dimension<2, 1, -2, 0, -1, 0, 0>;

} // namespace dimSafe
